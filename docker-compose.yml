# Root orchestrator — delegates to each app's own docker-compose.yml
# Usage: docker compose up -d
#
# Each app's compose file is resolved with its own directory as the base path,
# so volumes, build contexts, and .env files all resolve correctly.

include:
  - apps/api/docker-compose.yml
  - apps/web/docker-compose.yml
