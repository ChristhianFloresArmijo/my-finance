{
  "name": "my-finance",
  "private": true,
  "scripts": {
    "dev":   "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm -r build",
    "lint":  "pnpm -r lint",
    "test":  "pnpm -r test",
    "api":   "pnpm --filter api dev",
    "web":   "pnpm --filter web dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}
