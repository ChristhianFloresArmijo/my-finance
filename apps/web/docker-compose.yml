services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    tty: true
    stdin_open: true
